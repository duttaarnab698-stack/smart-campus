document.querySelectorAll('body[data-page="energy"] .energy-chart-grid .bars').forEach(chart=>{const rows=[...chart.children],values=rows.map(row=>Number(row.querySelector('label b')?.textContent||0)),max=Math.max(...values,.01);chart.innerHTML=rows.map((row,index)=>{const label=row.querySelector('label')?.childNodes[0]?.textContent?.trim()||'';const value=values[index];return `<div class="bar-column"><span class="bar-value">${value.toFixed(2)}</span><i><em style="height:${value/max*100}%"></em></i><label>${label}</label></div>`}).join('')});
